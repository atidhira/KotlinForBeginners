<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/item_name"
            style="@style/Header"
            android:padding="5dp"
            android:text="@string/numbers" />

        <LinearLayout
            style="@style/LinearViewTheme"
            android:orientation="vertical">

            <TextView
                style="@style/LinearViewDarkTheme"
                android:padding="5dp"
                android:text="@string/numbers" />

            <TextView
                android:id="@+id/tvNumbers1"
                style="@style/TextItem"
                tools:text="Isi" />

            <TextView
                android:id="@+id/tvNumSyntax"
                style="@style/TextItem.Syntax"
                tools:text="syntax" />

            <TextView
                android:id="@+id/tvNumbers2"
                style="@style/TextItem"
                tools:text="Isi" />

        </LinearLayout>

        <LinearLayout
            style="@style/LinearViewTheme"
            android:orientation="vertical">

            <TextView
                style="@style/LinearViewDarkTheme"
                android:padding="5dp"
                android:text="@string/operasi_pada_numbers" />

            <TextView
                android:id="@+id/tvOperation"
                style="@style/TextItem"
                tools:text="Isi" />

            <TextView
                android:id="@+id/tvOprSyntax"
                style="@style/TextItem.Syntax"
                tools:text="syntax" />

        </LinearLayout>
    </LinearLayout>
</ScrollView>